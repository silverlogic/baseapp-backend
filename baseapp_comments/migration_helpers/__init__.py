"""
Helpers for comments-related migrations.

This package intentionally hosts reusable migration utilities that can be imported by
project-level migrations (for example in `testproject/comments/migrations`), so legacy
projects can reuse the same data migration logic when adopting newer comments schema
patterns.
"""
